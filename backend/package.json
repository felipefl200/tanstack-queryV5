{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx json-server --watch db.json --port 8000",
    "start": "node server.js"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
